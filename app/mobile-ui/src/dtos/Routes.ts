export enum SupportedRoutes {
  base = "/",
  home = "/home",
  welcome = "/welcome",
  exercise = "/exercise",
  exerciseResult = "/exercise-result",
  settings = "/settings",
  legal = "/legal",
  legalDetails= "/legal-details",
}
